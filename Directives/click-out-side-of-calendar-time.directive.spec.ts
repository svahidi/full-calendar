import { ClickOutSideOfCalendarTimeDirective } from './click-out-side-of-calendar-time.directive';

describe('ClickOutSideOfCalendarTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutSideOfCalendarTimeDirective();
    expect(directive).toBeTruthy();
  });
});
